# Weather API Python Project 🌤️

This Python project fetches real-time weather data for any city using the **OpenWeatherMap API**.  
The script takes a city name as input and displays weather details like temperature, humidity, and description.

---

## 🔹 Features

- Fetch weather information by city name
- Displays:
  - City name
  - Temperature (°C)
  - Humidity (%)
  - Weather description
- Simple and interactive command-line interface
- JSON response parsing

---

## 🔹 Prerequisites

- Python 3.x installed
- `requests` library
- OpenWeatherMap API key

Install `requests` library if not already:

```bash
pip install requests

